interface:
  display_name: "Frontend Implementation"
  short_description: "Build and verify browser frontends"
  default_prompt: "Use $frontend-implementation to build the requested frontend and verify it in a real browser."

policy:
  allow_implicit_invocation: true
